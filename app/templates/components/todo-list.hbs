<div class="jumbotron text-center">
{{!-- 	<h2>List</h2>
	<div class="row">
	  {{#each model as |record|}}
	    <div class="col-md-4">
	      <div class="panel panel-default library-item">
	        <div class="panel-heading">
	          <h3 class="panel-title">{{record.title}}</h3>
	        </div>
	        <div class="panel-body">
	          <p>Address: {{record.elements}}</p>
	        </div>
	        <div class="panel-footer text-right">
	          {{#link-to 'libraries.edit' library.id class='btn btn-success btn-xs'}}Edit{{/link-to}}
	          <button class="btn btn-danger btn-xs" {{action 'deleteLibrary' library}}>Delete</button>
	        </div>
	      </div>
	    </div>
		{{/each}}
	</div> --}}

	<h2>List</h2>
	<button class="btn btn-success btn-xs" {{action 'addRecord'}}>Add</button>

	<input type="text" class="form-control" id="usr">

	<div class="row">
		{{#each model as |record|}}
	    <div class="col-md-4">
	      <div class="panel panel-default library-item">
	        <div class="panel-heading">
	          <h3 class="panel-title">ToDO title - {{record.title}}</h3>
	        </div>
	        <div class="panel-body">
	          <p>Something todo</p>
	          <p>smaller thing to do</p>
	        </div>
	        <div class="panel-footer text-right">
	          <button class="btn btn-success btn-xs" {{action 'editRecord' record}}>Edit</button>
	          <button class="btn btn-danger btn-xs" {{action 'deleteRecord' record}}>Delete</button>
	        </div>
	      </div>
	    </div>
	    {{/each}}
	</div>
</div>
